/**
 * Store Types Barrel Export
 * 
 * Shared type definitions for store feature.
 * These types can be imported in both Server and Client Components.
 */
export type {
	SerializedProduct,
	SerializedPagedResult,
	SerializedCategory,
	InitialStoreData,
	StoreSearchParams,
} from './serialized'

